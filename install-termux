if [ -d "resources" ]
then
    echo "Current directory Identified.."
else
    apt install git -y
    git clone https://github.com/manuop1234/NOVALIGHT-USERBOT
    cd Light
fi
bash resources/startup/termux.sh
